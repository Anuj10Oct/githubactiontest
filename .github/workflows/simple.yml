name: Shell Commands

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: echo a string
        run: eccho "First Github Action"
      - name: multiline script
        run: |
          node -v
          npm -v